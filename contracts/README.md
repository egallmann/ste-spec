# Cross-Component Contracts

This directory is the canonical home for published runtime/kernel handoff
contracts.

Published contract artifacts:

- `architecture-evidence.schema.json`
- `kernel-admission-assessment.schema.json`
- `examples/`

These files define serialized shape only. Behavioral rules and semantic
constraints live under `invariants/`. Architectural rationale lives under
`adr/`.
